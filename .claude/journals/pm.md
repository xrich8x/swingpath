# pm — working journal

**READ THIS FIRST IF YOU ARE RESTARTING.** A usage limit kills an agent outright and
nothing restarts it automatically. Whatever is below is what survived.

**Write here DURING the work, after every meaningful step** — a finding, a decision, a
command whose result you would not want to re-derive. You can only write when you call a
tool, so you cannot stream your thinking: the goal is that a kill loses ONE step, not the
whole run. Rewrite TASK/STATE in place; append to LOG; compact LOG when it passes ~30 lines.

This is transient working state. Durable learnings go in `.claude/agent-memory/pm/`, and
findings go in `docs/STATE.md` + `docs/evidence/`. Do not duplicate those here.

---

## TASK — what I was asked to do

2026-08-29. ONE task: **re-sequence the queue from scratch.** Old queue empty (1,2,3,5
shipped; 4 bounce_hypothesis FAILED; 6 court-correspondence + 7 activity-gate ruled out by
me for unattended work). Deliver: (1) ranked queue, each with reason/owner/drop-condition;
(2) MY call on researcher's motion test (~0.5 conf, minutes of compute) + whether the
contrast measurement rides along; (3) explicit NOT-queuing list, esp. human-blocked items
batched for founder; (4) what must not run unattended.
No code. May write journal + agent-memory. Hand STATE row text to lead, do not apply.

## STATE — where I got to

Read: journal (empty=fresh), pm MEMORY.md, mobile-parity-first, full
far-player-motion-contrast evidence, full docs/STATE.md. DECISIONS MADE — writing up.

**The headline I found in STATE:** the v1 critical path is 100% founder-blocked.
P0-0 (Core ML export) needs a Mac; P0-2 (pose affordability on A13 — the top runtime
risk, pose binds runtime at ~1000 ms/frame) is blocked ONLY by re-clicking yt_match40's
four corners. Everything I can queue is OFF the critical path. That reframes the human
batch from chores to the only thing that moves v1.

**Ranked queue (decided):**
1. Calibration audit sheets for every *_pts.json — LEAD, inline, no agent slot. Starts
   the founder's clock; converts the biggest blocked item into a 10-min look.
2. Motion test + contrast, 15 frames — BACKEND-DEV. Does not contend with (1).
3. Point-boundary labelling protocol — RESEARCHER. Must precede the founder's 3-6 h.
4. gate_ball_to_court characterisation — head of the PARKED ball lane.
5. Speed coverage (chain) — parked; unparks FIRST if founder's TrackNet idea is
   detector-only. That question is my top open question.
6. Joint line-to-model correspondence — court is NOT v1-blocking (manual tap gives the
   homography). 4-8 sessions, can fail, needs a human eye. Below the cut for v1.
7. Audio rolling-floor Swift port — infrastructure for a feature with no measured value.

**Motion test call: RUN NOW, contrast RIDES ALONG.** Same 15 frames, same load, no
homography, no new labels — two runs for one load is waste. CRITICAL CONSTRAINT:
contrast gets NO gate (none pre-registered) — descriptive only, or it is a rule-2
violation. And the outcome is ASYMMETRIC: a FAIL is final unattended, a PASS is
PROVISIONAL until a human sees frames (the near blob could be a shadow, the near
player, or a shivering fence panel).

**Cutting:** far-court recall labelling (4,087 frames / 4-5 h) — that human time goes
to point boundaries instead; score layer is a shipping requirement, far-court recall
improves line calling which is PARKED.

## LOG — newest first

- No STATE row is owed by this run: I measured nothing, and plans do not get rows. I am
  handing the lead PRE-WRITTEN rows for the motion test in BOTH directions so the kill
  condition cannot be softened after the result is known.
- Read STATE in full. Confirmed: correspondence gate pre-registered + unblocked but court
  auto is not needed for v1 parity (manual tap, my own mobile-parity-first memory).
  Rule 6 closes detector work — so localised-weighting confirm (2h20 GPU) and the
  more-data question are both dead spends, not queue items.

## LOG — newest first

- Read researcher's evidence file in full. Key: mechanism already failed BACKWARDS-SIGN in
  aggregate (feet_in_court, 216 locks/30 clips, gap -0.033..-0.071); ~9 median mover blobs
  per frame is the load-bearing negative; contrast has ZERO measurement; per-frame identity
  question genuinely untested; gate pre-registered w/ mandatory random-blob null control.
