# Off-machine backup — second disk VERIFIED independently, off-machine still open

> Evidence for the `off-machine-backup` row in [docs/STATE.md](../STATE.md) (Open).
> Text preserved verbatim from SCOREBOARD.md at the 2026-08-26 split.

Re-verified 2026-08-17 with a new `tools/backup_offmachine.py --verify-only`: `train_clips` **21/21** and `ball_dataset` **73,098/73,098** all present and size-matched at `C:\SwingPath_Backup`, confirming the earlier SHA256 claim by an independent route. **Scope correction while doing it:** `data/gold` is deliberately NOT in the tool's target list — the part that matters, all 31 `*.labels.json` holding the 2,159 human clicks that cannot be regenerated at any price, is **tracked in git and pushed to the private GitHub remote**, so it is already off-machine; the other ~3,185 files there are regenerable frame images. Including it made the checker report 3,258 "missing" files against a perfectly good backup, which is worse than no checker. **STILL OPEN and needs hardware I cannot supply: the copy is on the same machine**, so fire, theft and ransomware remain uncovered. Run `py tools/backup_offmachine.py --dest <removable drive>` when one is plugged in. The tool refuses a destination inside the repo, warns when the destination looks like an internal disk, and verifies by content rather than trusting an exit code (SCOREBOARD already records `robocopy` exiting **1 on success** and a wrapper reporting a false failure).
