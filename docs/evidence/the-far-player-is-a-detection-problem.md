# The far player is a DETECTION problem on the target footage

> Evidence for the `the-far-player-is-a-detection-problem` row in [docs/STATE.md](../STATE.md) (Open).
> Text preserved verbatim from SCOREBOARD.md at the 2026-08-26 split.

**Settled 2026-08-17 across all three calibrated clips, correcting the sharper-sounding claim first filed here.** `--far-player-rescue` does recover far-player frames the shipped guard then deletes — 37.2% -> 0.0% on yt_rally2 via depth-blind pixel radii — but fixing the guard (`body_relative`, see the dead-end table) changes the outcome on **1 of 3 clips only**. On yt_match40 the far player is detected on **14.5%** of frames and on am_hard_utr **26.7%**; both are under the 50% a path integral needs regardless of any filter, and yt_match40 is under the guard's own 15% floor before filtering starts. **So the lever to pull is more far-player detections, not a smarter guard.** `--pose-quality accurate` is already a measured negative on this axis (0.0 -> 0.0). Unexplored: a far-court tile detector for POSE analogous to `--far-ball-tile`, or accepting a permanent `None` with the coverage percent shown, which is what ships today. **One live misreport to fix first:** the guard's 15% floor ZEROES `stats.player_track_coverage`, so am_hard_utr reports 0.0% when perception saw a far player on 26.7% of frames. Distance stays `None` either way, but the percent shown next to "not tracked" is wrong. Separating "suppress for rendering" from "report what we saw" changes skeleton drawing and wants its own gate.
