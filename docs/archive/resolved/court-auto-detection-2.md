# Court auto-detection — REOPENED 2026-08-20 as a MASK problem, still closed as a model problem

> Evidence for the `court-auto-detection-2` row in [docs/STATE.md](../../STATE.md) (Open).
> Text preserved verbatim from SCOREBOARD.md at the 2026-08-26 split.

**Tier 1 (`courtfit` consensus) auto-accepts 11 of 20 gold clips with a perfect precision record** (3.4–13.9 px, zero wrong courts ever accepted); the 6/8 bar is verified correct. CourtNet (Tier 2, 20.2% held-out detect) is the weaker path and is still not worth improving — **that closure stands**. What is newly open is the classical mask, at the user's direction, and Stage 0 has now measured what the 9 refusing clips actually fail on (`eval/run_eval.py`, evidence data/output/court_stage0_baseline.md). **The premise that the detector needs four visible corners is FALSE and measured so:** corners are a parameterisation projected from a 6-DOF camera fit, and **9 of the 11 clips that DO auto-calibrate have a corner off-frame (up to 224 px out)**, including the court-level mount at 8/8 votes — while **both hard refusals have all four corners in frame**. The real families, from the overlays: **(1) clay — the mask fires ~entirely OFF the court** (`am_wingfield_clay` 3,092 mask px, essentially all foliage/fence; `am_rally32short` 3,185, same), because the line signal on clay is chroma and `line_ridge_mask` discards it at `sat < 90` while its luminance ridge fires on tree edges — so **CLAHE alone cannot fix it, chroma must enter the mask**; **(2) low indoor — architecture outcompetes the paint** (`am_ntrp45w`, the worst wrong court at 111.0 px, has 16,502 mask px / 29 lines dominated by rafters, strip-lights and a curtain edge, and collapses the court onto the horizon on **8 of 8 frames** — confident and wrong, saved only by the agreement rule); **(3) marginal agreement** (`am_beginner` 4 votes/30.1 px, `am_ntrp50` 5/69.1, `am_indoor_hard1` 2/24.9), not yet characterised. Waiting on: clay/shell frames in `eval/frames/` from the user — `data/gold_clay.mp4` and `data/gold_shell.mp4` are **ball**-labelled only, so no clay or shell surface beyond the two gold clay clips is measurable today, and multi-sport overlay footage does not exist in the repo at all. | **BREADTH RUN 2026-08-20 over ALL 54 distinct recordings in the repo** (66 files deduped by source recording; evidence data/output/court_breadth_54.md). On the product's own footage — one fixed camera, one court — **17 of 43 (39.5%) auto-accept**, against 55% on the gold set, i.e. the gold set is easier than the repo at large. **Broadcast is 0 of 10 and 9 of those lock ZERO frames** — not vote disagreement, the detector never fires; that is out-of-scope footage for Tier 1, not a regression. By surface, **clay is the binding problem at 1 of 7 (14%) while shell/pale is 2 of 4 (50%)** — `gold_shell` accepts at 8/8 votes — so **white-on-light is NOT where this breaks and Stage 2 should target clay only**; a pale surface still yields a luminance ridge, clay's signal is chroma. **PRECISION HOLDS ON UNSEEN FOOTAGE, measured not assumed:** 10 clips carry a human-placed calibration (`"_exact": true`, the Court Setup tool's deliberate-placement marker; `eala_pts_auto` excluded as self-grading), and the single acceptance among them is **correct at 14.3 px@640** while **five of the nine refusals would have been wrong by 20.8-59.6 px** at a lower bar. Read 1-of-10 as difficulty, NOT recall — those clips have a hand calibration precisely because auto-detection failed on them. **RETRACTED within the same session:** that run's `am_hard_utr` acceptance was first called a wrong half-court lock from the overlay picture; measured against the human placement it is the SAME court (near-baseline width 2132 vs 2147 px, far-baseline y within 5 px) — an overlay eyeball cannot separate a correct low-mount fit from a half-court error, and the other 16 acceptances stay unverified rather than eyeballed. **WHY IT FAILS, measured at the KNOWN-TRUE court 2026-08-20** (data/output/court_why_it_fails.md). Scoring `autodetect`'s own gates on the ten HUMAN-placed calibrations splits the failures **five and five**: **(A) the accept gate REJECTS the correct court** — g at the human's own court is **0.18-0.31 against the 0.33 bar** on 5 of 10, so no search can help; `am_hard_utr`'s truth scores 0.314 and FAILS while the clip is accepted, meaning the shipped court outscores the correct one, which is why its error is 14.3 px not ~0. **(B) the correct court passes every gate and is still not found** — g 0.44-0.65, structure 0.88, sufficiency satisfied, yet only 2-4 of 8 votes. **So half the gap is scoring and half is search; a fix aimed at one addresses at most half.** THREE hypotheses tested and KILLED: broadcast-pose seeding (27 added poses, no change), court-too-small-in-frame (crop-and-upscale x1.18-x2.50, no change, and `BwLUgip8OSI` got worse), and top-down-only frame selection at the user's request (`eval/pick_view.py`, human-chosen TV camera on 6 broadcast clips: **0 of 6**, with `eala_segment` already 23-of-24 top-down and locking zero). A fourth reading was **RETRACTED mid-run**: a wide sweep found g=0.41-0.52 courts on broadcast, above the gate, which read as 'search misses a reachable answer' — drawn on the frames all four are junk (three imply a 0.6-0.9 m camera at Wimbledon, 21-37 px cam_fit residuals), so g's global maximum sits on a WRONG court. **Separate defect found en route:** the coarse grid searches far-half-width 0.20-0.42 of frame width while **30 of 30 human-measured courts sit at 0.09-0.22** — the grid searches poses that do not occur and the learned prior is doing the seeding, consistent with failure wherever the prior has no coverage. **The user's 'more court shown does worse' is NOT testable on the gold set** (all 20 clips show 94-100% of the court); on frame-area it leans the right way (accepted 0.21-0.40 of frame, all 4 clips below 0.21 refused) but at n=5 per group it is a lean, and the zoom experiment says apparent size is not itself causal. **FIRST FIX ATTEMPTED AND REJECTED ON ITS GATE 2026-08-20.** The grid defect above was real, so it was fixed and measured: `COARSE_GRID` hoisted out of `autodetect` (pure refactor — the baseline arm reproduces the committed scorecard exactly), then three arms one variable at a time against a gate registered BEFORE running (accepted >= 11/20 AND zero accepted court over 20 px). **All three FAIL: A_wf 13/20 but admits am_beginner 26.1 px and am_indoor_hard2 77.7 px; B_wn and C_both 12/20 admitting am_beginner ~25 px.** No arm loses an existing clip and the widened grid does reach courts the old one could not — **every clip it newly reaches, it gets WRONG**, and 77.7 px is a different rectangle, not a near miss. This would have been the first wrong court ever auto-accepted on this gold set. NOT SHIPPED; the grid keeps its values. **The result CORRECTS the five/five split above:** the half called a search failure is really scoring — extend the search to reach those courts and the scoring accepts a wrong one, so reachability was hiding it. **Fix the agreement score BEFORE widening the search; widening it first costs the precision record.** **MASK FIX ALSO BUILT, MEASURED AND REJECTED 2026-08-20** (evidence section 7). A candidate mask (`eval/masks_candidate.py`, never in backend/) lifts the true court above the accept gate on the search-free proxy `eval/score_truth.py`: clips whose g@truth is under 0.33 fall **11/30 to 3/30**, and the two clay clips scoring literally **0.000 at the human's own court** reach 0.20-0.32. **Chroma and CLAHE are REDUNDANT SUBSTITUTES** - either alone gives the whole gain, both gives no more, neither reproduces the baseline; reading the single ablations alone said 'neither matters', an artefact of removing one while the other covered for it. **But the product gate FAILS on all three arms: chroma_only 9/20, clahe_only 13/20 with two accepted courts at 22.4 px, both 6/20.** Nothing ships. **THE PROXY DID NOT PREDICT THE PRODUCT** - all three are indistinguishable on score_truth (28/30) and span 6/20 to 13/20 on the gate, because the proxy asks only whether the criteria RECOGNISE a court handed to them, not what the search finds nor how the four downstream stages react; it is a screening tool, never a gate. The 22.4 px pair are visibly the SAME court loosely fitted, not the different-rectangle failure the grid sweep produced, and 20 px was drawn across a gap the gold set had no data in - but the gate was pre-registered and **the line does not move after the fact**; court-outline IoU was tested as a principled replacement and **does NOT separate** (0.782 near-miss vs 0.714 genuinely-wrong, against the pixel metric's 11.6 px gap). **THE GENUINE POSITIVE: `am_rally32short` (clay) auto-calibrates in ALL THREE arms - no clay clip had ever auto-calibrated on this gold set.** The collateral on hard courts is what fails it (clahe_only loses 1, chroma_only 4, both 7), so the next candidate must be **ADDITIVE - fuse the new channel only where the shipped mask has no evidence - not a global swap.** **SHIPPED 2026-08-20 — SURFACE ROUTING, the first change here to clear the gate (evidence section 8). 11/20 -> 12/20 accepted, NOTHING lost, zero wrong courts, median error 8.3 -> 8.1 px.** The user's proposal: judge the surface, then use the mask built for it, instead of one mask for every surface. Sections 6 and 7 each failed by trading one surface's gain for another's loss; routing removes that mechanism because a non-clay frame takes a **bit-identical** path (pinned by test_court_surface_routing.py). Surface is separable from colour alone — **clay a* 148.0-163.5 vs 132.0 max for everything else**, `CLAY_A_STAR = 140.0` in the middle of that gap, **3 of 3 clay gold clips right and zero false positives on the other 17**. **SHELL IS DELIBERATELY NOT SPECIAL-CASED** — measured to work already (`gold_shell` at 8/8 votes), a pale surface still yields a luminance ridge; one fewer detector than proposed, on evidence. Routing to the EXISTING `_clay_mask` was measured FIRST and is identical to baseline (gained none, lost none) — `autodetect` already falls back to it, so a better clay mask was needed, not an earlier reach for the old one. New `calibration.clay_line_mask`: CLAHE ridge on L*, no saturation gate, Hough structure-clean. **Re-run against the shipped call sites reproduces the monkeypatched sweep exactly.** On the 7 clay recordings outside the gold set, accepted **1/7 -> 3/7** and `tnxkujogch4` goes from **0 of 8 frames locking to 8 of 8**; all three acceptances rendered and verified correct by eye. CLAHE ships over chroma because chroma LOSES `sAjkpeRq4P4` (6 votes -> 5). **Two honest limits: the three accepted clay clips look like ONE CLUB** (same house, windbreak, treeline), so read it as one venue family rather than three; and it is **one clip on a 20-clip gate** — the supporting breadth is total clay votes 15 -> 27 with 5 of 7 clips improving and 2 slightly worse. **BREADTH + REFERENCE RE-RUN after shipping (section 9): 17/54 -> 19/54 recordings, 17/43 -> 19/43 on the product's own fixed-camera footage (39.5% -> 44.2%), and NOT ONE CLIP ANYWHERE LOST ACCEPTANCE.** Every non-clay surface is bit-identical by construction - shell 2/4, indoor 2/6, night 2/3, low-mount 2/4 all unchanged - so the gain is entirely clay, **1/7 -> 3/7**. **Broadcast stays 0/10 exactly as predicted**: three hypotheses were tested there and none implicated the mask. Against the ten HUMAN-placed calibrations (the harder subset - they have hand calibrations because auto-detect failed on them) acceptance **1/10 -> 2/10** and **`sAjkpeRq4P4` lands at 2.3 px@640, tighter than anything in the gold set's accepted band (3.4-13.9)**, while `CYqapSq5llo`'s fit improves 3x (51.6 -> 16.9 px) without yet crossing the vote bar. **The cost, stated:** two clay clips lose votes without losing acceptance (`CYqapSq5llo` 3->2, `SgZpQtiTG1A` 3->1, the latter now reaching a court only via the stacked rescue) against +7, +5 and +2 elsewhere - five of eight clay recordings improve, two worsen. **The clay gain is CONCENTRATED: `gold_clay`, `tnxkujogch4` and `sAjkpeRq4P4` share a house, windbreak and treeline and are very likely ONE CLUB**, so read it as one venue family plus `am_rally32short`, not five independent venues. **CORRECTION 2026-08-20 (section 10), found while rendering the before/after pictures: THE GOLD SET AND THE DROP SET ARE NOT INDEPENDENT — 9 of the 20 court gold clips share a source video with the 54-recording drop set**, and `am_rally32short` IS `yt_tnxkujogch4.mp4` renamed (dHash 3 bits). **This is trap T17 recurring**: a clip renamed into the gold set, identity matched on filename; `collect_frames` deduped by filename and YouTube id and never consulted the gold manifests, which record the source. **Section 9's claim that the gate gain was a separate venue from the breadth gain is WITHDRAWN — they are the same recording counted twice.** Restated: routing newly accepts **two distinct recordings** (`tnxkujogch4`/`am_rally32short`, and `gold_clay`) plus `sAjkpeRq4P4` improving, and by eye **all three are the same club**. The clay evidence is ONE CLUB, not five venues. **Unchanged:** the gate itself (12/20, nothing lost, zero wrong, reproduced against the shipped call sites), the bit-identical non-clay path, and the measured 2.3 px fit on `sAjkpeRq4P4`. What is weakened is breadth, not correctness — the next step is clay from unrelated venues, and any future population split must key on the manifest `video` field, never on clip names. **FIXED (section 11): recording identity now derives from the SOURCE VIDEO the gold manifest records, never the clip name.** `eval/recordings.py` gives every gold clip and drop group one canonical key and exposes `overlap()` / `independent_drop_groups()`; `run_eval --drop` and `collect_frames` both print the independent subset and name the exclusions, and 4 tests fail if identity regresses to names. **Two figures reported earlier are CORRECTED: the breadth gain is +1 independent recording, not +2** (`tnxkujogch4` IS the gate gain `am_rally32short`), **and the drop set's baseline acceptance is 10/45 = 22.2%, not 17/54 = 31.5%** — the nine duplicates are gold clips and seven already pass, so removing them LOWERS the rate. 11 gold clips are youtube-stream with no local file and are genuinely independent. **The gate is unaffected** (it was always the 20 court gold clips alone): 12/20, nothing lost, zero wrong stands, as does the 2.3 px fit on `sAjkpeRq4P4`. What shrinks is the independent support for the clay work — **one recording beyond the gate, at the same club as the gate's own gain** — so clay from unrelated venues is the single thing that would move this from 'works on one club' to 'works on clay'. **WITHDRAWN 2026-08-20 (section 12): 'shell already works' is FALSE on the target footage.** Five new Philippine indoor shell recordings (Manila Polo Club, Flexi League, Hillsborough; 4K, cut into 58 clips by `tools/split_by_serve.py`, grouped to 5 recordings via lineage) score **0 of 5** — and `l4CAptCem88` locks 7 of 8 frames while scoring 1 vote, i.e. it finds a DIFFERENT court every frame. Section 8's claim rested on 4 clips that were all BRIGHT pale courts; the population this project targets is dim and indoor. **`court_surface` also called all five 'hard'** — `SHELL_L = 166` was fitted to L* 170-176 and these measure **L* 119-164**, with Manila Polo at **a* 137 against a clay line of 140**; the rule captures 'bright and pale', not 'shell'. Harmless today (shell and hard share a mask) but not to be trusted if shell ever gets its own. **THE CAUSE IS NOT THE SURFACE:** the masks visibly contain the court lines; what drowns them is the BUILDING — roof trusses, strip lights, fence lattice — at 395k-1,257k mask px and 16-40 distinct lines of which the strongest are architecture. **This is failure family 2 (`am_ntrp45w`, 111 px wrong) in force**, so a better shell mask cannot fix it. The next lever is line SELECTION by ground-plane position: the court is on the ground, the trusses are above the horizon, and `_detect_lines` uses no such prior.
