# STATE.md - the state of play

**This file is the living record. Update it in the same commit as the work it
describes.** Split out of SCOREBOARD.md on 2026-08-26: the verdicts stayed here,
the mechanisms and war stories moved to [evidence/](evidence/).

**Never create a new markdown file to record a result.** A result is a row here plus
a file in evidence/. If you are about to write NOTES.md, FINDINGS.md, SESSION_N.md
or SUMMARY.md, you want one of those two places. New top-level docs require a line
in CLAUDE.md's doc map, and that file has a hard cap - so a new doc costs an old one.

**This is enforced, not remembered.** `.claude/hooks/state-guard.sh` runs before
every `git commit`: if the commit changes code (`backend/`, `tools/`, `frontend/src/`,
`mobile/`, `ball_physics/`) and this file is not also modified, the commit is refused.
Doc-only, data-only and config-only commits pass. For a change that genuinely moves no
number, put `[no-state]` in the commit message.

**When to update:** shipped something that moved a number -> a row in *What has worked*.
Measured something that did not -> a row in *What has not worked*, with the number and
the reason. Changed a model/runtime/hardware -> *The stack*. Got burned by a process
mistake twice -> [TRAPS.md](TRAPS.md).

**The one formatting rule**, and it exists because ignoring it cost three corrections:
an **Open** row may NOT restate a number another table owns - it cites the row instead.
One number, one home.

## How to read every number here

- **Measured against independent human labels**: 1851 ball clicks + 308 no-ball frames
  across 10 clips, test-only, never trained on. A model never grades its own homework.
- **One declared exception:** rows quoting a *HUD MAE* or *HUD speed error* are measured
  against SwingVision's burned-in MPH panel. Those are **agreement with another
  estimator, not accuracy**. `tools/synth_truth.py` is the compliant speed reference and
  the only source of absolute accuracy here. Do not add new HUD-referenced numbers.
- The full discipline, including the 11 working rules this file used to carry, is in
  [ML_PRACTICES.md](../ML_PRACTICES.md).

---

## The stack

| Layer | What we use | Notes |
|---|---|---|
| Ball detection | **BallNet v21** — in-house TrackNet-style heatmap CNN | 512×288 input, 3-frame stack, ~2 MB. Default detector. |
| Ball fallbacks | TrackNet (vendored), WASB | Kept for benchmarking; not the shipped path. |
| Player pose | YOLO-pose via ultralytics | `fast` preset (yolo11m@1280) default; `accurate` (yolo11x@1920) for the far player. |
| Court | Classical line-fit + consensus, CourtNet seam | Manual fallback via the setup tool; auto-detect is fragile on low cameras. |
| Geometry | Homography, ballistic fits — closed-form | Never learned. This is a hard architectural boundary. |
| Smoothing | Constant-acceleration Kalman + RTS smoother | Image space. Interpolates only, never extrapolates. |
| Backend | Python 3.12, NumPy/SciPy, OpenCV, PyTorch | `backend/.venv` (CPU) and `backend/.venv-train` (CUDA). |
| Frontend | React + Vite | Reads `match.json`; the schema is the only contract. |
| Training HW | One RTX 5060 Ti | **Single GPU — one job at a time.** Enforced by `lab_jobs.py`. |
| Shipped inference | CPU-first, ~0.7–1.1 s/frame | Offline-first by design; there is no real-time requirement. |
| Mobile | ONNX + int8, argmax baked into the graph | 0.9 MB / 11 MB. Call logic ported to JS, verified bit-identical. **The int8 ball graph fails its parity bar on 3 of 6 gold clips** - see the Open row before treating it as shipped. |

**Footage layout (reorganised 2026-08-20):** every source video lives under
`data/incoming/<surface>/` - **Clay 9, Hardcourt 39, Shell 64, Grass 4** (counted
2026-08-28; the old "Shell 6" predated `split_by_serve.py` cutting the five Manila venues
into per-point clips, and 88 of the 116 carry audio - `data/output/audio_census.json`), plus
`Raw - Do Not Process` (9 full-length downloads whose trims are already in the surface
folders; the eval skips it by rule, because sweeping both counts the same court twice).
`data/amateur_clips`, `data/train_clips`, `data/gold_clips` and `data/highlights` are gone.
**FILENAMES ARE NEVER CHANGED** - the ball gold-leak guard and `lineage.json` key on
basename, so a rename silently defeats both (trap T17); only directories moved.
`tools/backup_offmachine.py` TARGETS moved with the footage - a backup target pointing at
an emptied directory reports a clean backup while covering nothing. **One safety cue was
traded for documentation:** `data/gold_clips` used to keep gold videos out of the training
pool by LOCATION; filed by surface they now sit beside training footage, so
`data/incoming/README.md` lists all 19 gold files explicitly. The guard still catches a
leak (it derives from the manifests, not a folder) but a human has no visual warning.

**Target footage:** amateur phone video on a fence or tripod, 720p–1080p, 30–60 fps,
often a low mount. Measured mounts: **1.38 m** and **1.74 m**. That is the constraint
that shapes everything — a low camera limits *measurable depth* to 22–32% of the court.


---

## What has worked

Ordered roughly by how much it moved.

| Change | Effect | Evidence |
|---|---|---|
| **Rendered-corner audit for every calibration** (`tools/render_corner_audit.py`) | **27 of 29 `*_pts.json` rendered** onto a real frame with the clicked corners named and the quad drawn - the T23 check that a residual cannot do. Draws **nothing derived from the homography**: a projected court is a consequence of the clicks, so checking clicks against it is circular. `yt_match40` is **visibly, grossly wrong** (all four corners on the surround and the hedge) while stamped PASS at 0.9 px - the tool reproduces T23 on sight. The **camera-height fit is the one screen that isolates it**: `yt_match40` alone fits an implausible **11.3 m** for a court-side mount; every other clip fits 1.3-3.4 m. 2 skipped (`court`, `yt_court` - no matching video) | [evidence/p0-3-crop-around-contact.md](evidence/p0-3-crop-around-contact.md) |
| **P0-3 context sheet** (`tools/p0_3_context_sheet.py`) | renders each far-end contact as full frame + blown-up crop from the probe's own JSON, no model run. Deliberately draws **no court lines** - `yt_match40`'s calibration is wrong (T23) and would hand the reader a near/far cue derived from the broken thing. Turned P0-3 from PROVISIONAL to reviewed | [evidence/p0-3-crop-around-contact.md](evidence/p0-3-crop-around-contact.md) |
| **Calibration auditor finds videos recursively** | shell calibrations **0 pass / 5 fail -> 4 pass / 6 LOW-CAMERA / 0 fail**; residuals 41-117 px -> **0.0-2.5 px**, the best band in the repo | [evidence/making-the-calibration-auditor-find-videos-recursively.md](evidence/making-the-calibration-auditor-find-videos-recursively.md) |
| **Court refiner reach scales with resolution** (`max_move_px` 55 -> `55*w/640`) | **moves no number** (gold 12/20 -> 12/20); shipped as a correctness fix, an exact no-op on the gate | [evidence/scaling-the-court-refiner-s-reach-with.md](evidence/scaling-the-court-refiner-s-reach-with.md) |
| **Hard-negative mining + retrain** (v21 became default) | no-ball false-fire pooled **14% -> 6.0%** at flat recall | - |
| **Occlusion augmentation + visibility-weighted loss** | gold **82.9 -> 84.9**, occluded **84.2 -> 89.7** | - |
| **Fixing the court gate for resolution** | far-ball retention at 1080p **15.4% -> 100%** | - |
| **`suppress_false_locks`** (persistence + min-segment) | false-fire **61.5% -> 15.4%** on yt_rally2; costs 5.4-10 pts recall | - |
| **Kalman smooth + forecast** | jerkiness **9.9 -> 4.1 px/frame2** at -1.6 pt hit@10 | - |
| **Static-lock gate** | **zero** static junk locks; ball-only coverage went up | - |
| **Scaling every pixel threshold by `frame_height/720`** | exact no-op at 720p; stops silent far-ball deletion at 1080p | - |
| **Roll-aware court snap** | **6.9 -> 1.8 px** on a -2.4 deg clip; no-op when level | - |
| **demo30 re-calibration** | **564.6 px -> 0.5 px** fit residual - lowest in the repo | - |
| **Calibration audit + `_audit` stamp** | degenerate calibrations warn on load instead of failing silently | - |
| **The gold benchmark itself** | turned every claim in this project from an opinion into a number | - |
| **Synthetic ground truth** (`tools/synth_truth.py`) | the only **ABSOLUTE** accuracy here: line calls **95.9%**, bounce **0.75 m** median, drag **-21.7%** | [evidence/synthetic-ground-truth.md](evidence/synthetic-ground-truth.md) |
| **SwingVision scrub + trainer guard** | **83 pseudo-labels** sat inside overlay graphics; the guard refuses to train on an unscrubbed clip; keeps 11,104 of 11,187 labels | [evidence/swingvision-scrub-trainer-guard.md](evidence/swingvision-scrub-trainer-guard.md) |
| **`am_hard_utr` perception cache** | the clip that kills smoother tunings is testable at last - 14,499 frames, 10,840 locks, 120 shots / 79 rallies | [evidence/am-hard-utr-finally-has-a-perception.md](evidence/am-hard-utr-finally-has-a-perception.md) |
| **60 fps shipped as `--full-rate`** | **+5.8 pts** close-call accuracy at 1.5 m, arc reproj **148 -> 91 px**, against **2x perception time**. Opt-in; default unchanged | [evidence/60-fps-shipped-as-full-rate.md](evidence/60-fps-shipped-as-full-rate.md) |
| **The two JS mirrors are enforced, not asked for** | `tests/test_js_mirror_parity.py`; each guard **proved to fail** before being trusted | [evidence/the-two-js-mirrors-are-enforced-not.md](evidence/the-two-js-mirrors-are-enforced-not.md) |
| **Court TEST/TRAIN split + leak guard** | court numbers became measurable at all - **17 of 20** gold clips had been in training | - |
| **Camera-height curve** (`tools/height_curve.py`) | close calls **54% at 1.0 m -> 69% at 3 m -> 81% at 8 m** against a **56.2%** majority-class floor; bounce error 3.81 -> 0.37 m | [evidence/camera-height-curve.md](evidence/camera-height-curve.md) |
| **Frame rate isolated from detector dropout** | 30 -> 60 fps worth **+5.8 / +3.2 / +1.8 pts** at 1.5 / 3 / 12 m; bounce error **-24..-35%**. A *perfect* detector buys about the same | [evidence/frame-rate-isolated-from-detector-dropout.md](evidence/frame-rate-isolated-from-detector-dropout.md) |
| **Per-rally clips + highlights reel** (`run.py highlights`) | ffmpeg **stream copy**; deterministic ranking; the manifest records requested vs actual start | [evidence/per-rally-clips-highlights-reel.md](evidence/per-rally-clips-highlights-reel.md) |
| **Far court measured in METRES, not frame rows** | the `FAR_FRAC` proxy was wrong by **5-26x**; far-court queue **1,393 -> 2,677 gaps** off the same footage | [evidence/far-court-measured-in-metres-not-frame.md](evidence/far-court-measured-in-metres-not-frame.md) |
| **More labelled data, from more venues** | +57% frames buys **+5.6 pts** pooled recall (74.8 -> **80.4%**, 4.1 sigma), up on 9 of 10 clips. **False fire did not move. NOT shipped** - the chain test later failed | [evidence/more-labelled-data-from-more-venues.md](evidence/more-labelled-data-from-more-venues.md) |
| **The height guidance actually reaches the user** | `run.py check` + Court Setup tab now surface it; `check` calls `pipeline.calibrate_video` so it can no longer disagree with `analyze` (trap T15) | [evidence/the-height-guidance-actually-reaches-the-user.md](evidence/the-height-guidance-actually-reaches-the-user.md) |
| **Manual-correction UI** (Review tab + `run.py correct`) | edits FACTS only; score replayed through the one state machine. demo 2-5 -> 3-4; re-applying is a no-op | [evidence/manual-correction-ui.md](evidence/manual-correction-ui.md) |
| **Score and rally count stop pretending to be measured** | that layer has **no ground truth of any kind**; it now reports which rule split it - yt_rally2 **5 timeout / 0 tennis-rule** | [evidence/the-score-and-rally-count-stop-pretending.md](evidence/the-score-and-rally-count-stop-pretending.md) |
| **P/R/F1 at the field's threshold** | **reverses a verdict shipped hours earlier**: F1@4 **tracknet 57.8 / ours 48.5 / wasb 47.5**, TrackNet wins **9 of 10** clips. Ours is a recall-first detector, not a better one | [evidence/p-r-f1-at-the-field-s.md](evidence/p-r-f1-at-the-field-s.md) |
| **The environment that produced the numbers is written down** (`tools/freeze_env.py`) | exposed that the two venvs are an **opencv MAJOR version apart** - no experiment here has ever separated device from libraries | [evidence/the-environment-that-produced-the-numbers-is.md](evidence/the-environment-that-produced-the-numbers-is.md) |
| **CourtNet training seeded** | **no number moved**; removes a confound from the *next* A/B (trap T10) | [evidence/courtnet-training-seeded.md](evidence/courtnet-training-seeded.md) |
| **`distance_run_m` stops inventing a number** | the dashboard read a confident **0.0 m** for player B integrated over **0.0%** coverage. Now **None below the >=50% bar, never 0.0**, and refused outright in doubles | [evidence/player-movement-stats-stop-inventing-a-number.md](evidence/player-movement-stats-stop-inventing-a-number.md) |
| **BallNet v21 vs TrackNet vs WASB, re-dated** | pooled hit@10 **60.8 / 57.9 / 49.3** - **+2.9 pts, not the +10.5** an undated comment claimed; TrackNet wins outright on 2 of 10 | [evidence/ballnet-v21-vs-tracknet-vs-wasb-finally.md](evidence/ballnet-v21-vs-tracknet-vs-wasb-finally.md) |
| **BallNet v21 vs TrackNet at the CHAIN** - measured 2026-08-28 | **SPLIT; the default is UNCHANGED.** TrackNet takes solid ghosts **88 -> 62 (-29.5%)** for **8 hits** (0.31 hits per ghost, vs the ~7:1 structural rate) over 10 clips / 1658 clicks / 272 no-ball frames - and only **18%** of solid-ghost frames overlap, so it is a different model, not a shifted threshold. But BallNet wins or ties the PRODUCT half on both clips run end-to-end: **speed_confident 7 vs 5** and **22 vs 22**, more confident calls, longer trail. `event_audit` moves by 1 against its own **>=3** bar = INDETERMINATE. **H-FREE**: the court gate removes 0 locks on 7 calibrated clips x 2 arms, and `--no-gate` is byte-identical - so T23's broken `yt_match40` calibration cannot have touched it. No gate was pre-registered; this is a measurement, not a pass/fail | [evidence/ballnet-v21-vs-tracknet-at-the-chain.md](evidence/ballnet-v21-vs-tracknet-at-the-chain.md) |

---

## What has not worked

**Do not re-propose these.** Each was tried here and measured.

| Idea | Verdict | Evidence |
|---|---|---|
| **Building the court quad from the DETECTED LINES** | **Wrong in principle, not mis-tuned** - under perspective the two doubles sidelines converge, so they form no angular cluster. Best constructed quad **68-256 px** from truth vs the lattice's 7-20 | [evidence/building-the-court-quad-from-the-detected.md](evidence/building-the-court-quad-from-the-detected.md) |
| **Snapping a near-correct court onto the detected lines** | median distance from truth: seed 9.8 -> refiner **8.4** -> snap **70.5**. Needs JOINT line-to-model assignment, untested here | [evidence/snapping-a-near-correct-court-onto-the.md](evidence/snapping-a-near-correct-court-onto-the.md) |
| **Raising `topk` so more seeds get refined** | topk 12 -> 40 -> 150 moves no clip and costs **7x compute** (32 s -> 229 s per 4 frames) | [evidence/raising-topk-so-more-seeds-get-refined.md](evidence/raising-topk-so-more-seeds-get-refined.md) |
| **Removing the pose-prior weight from the seed RANKING** | no clip improves; the prior is not what buries the true seed | [evidence/removing-the-pose-prior-weight-from-the.md](evidence/removing-the-pose-prior-weight-from-the.md) |
| **Reachability** - that refinement cannot travel from seed to true court | **STOPPING RULE FIRED.** On **31 of 38** clips the nearest seed is already within reach. The brief predicted the opposite in advance | [evidence/reachability.md](evidence/reachability.md) |
| **Narrowing `EVID_BAND`** | **The band is INERT** - the hypothesis the whole brief was built around is refuted | [evidence/narrowing-evid-band-so-clutter-near-a.md](evidence/narrowing-evid-band-so-clutter-near-a.md) |
| **Observability from geometry instead of nearby paint** | **+0.000 margin** | [evidence/observability-from-geometry-instead-of-nearby-paint.md](evidence/observability-from-geometry-instead-of-nearby-paint.md) |
| **Behind-camera projection inflating the denominator** | **0.0%** of samples affected | [evidence/behind-camera-projection-inflating-the-denominator.md](evidence/behind-camera-projection-inflating-the-denominator.md) |
| **The low true-court score being human click error** | swept `tol` x0.5 -> x4; no clip shows the steep threshold a click-error explanation needs | [evidence/the-low-true-court-score-being-human.md](evidence/the-low-true-court-score-being-human.md) |
| **Player-foot gate + survivor-based vote rule** | **converts zero reference clips** | [evidence/player-foot-gate-a-survivor-based-vote.md](evidence/player-foot-gate-a-survivor-based-vote.md) |
| **The player-foot gate as a wrong-court negation criterion** | **DEAD - no discriminative power** | [evidence/the-player-foot-gate-as-a-wrong.md](evidence/the-player-foot-gate-as-a-wrong.md) |
| **The far player found by MOTION - the nearest blob as an IDENTITY signal** | **FAILS the pre-registered gate, 2026-08-29.** Nearest `movers.foot_points` blob to a POST-HOC far-player box: median **5.751** box-heights and **7 of 15** frames within 1.5, against a bar of <=1.5 on >=10 of 15. The mandatory random-blob null control also fails (median 9.265, 2/15; 0.0% of 1,000 seeded draws pass), so the negative is clean. Failure is **bimodal** - nothing between 0.62 and 5.75 box-heights: right on him on 7, and 173-632 px away on 8. Third measured negative in this family; per rule 3 do not propose a fourth | [evidence/far-player-motion-gate-result.md](evidence/far-player-motion-gate-result.md) |
| **Widening / height-scaling `AGREE_PX`** | the resolution artefact is real but widening does not recover the disagreement | [evidence/widening-height-scaling-agree-px-to-recover.md](evidence/widening-height-scaling-agree-px-to-recover.md) |
| **Trusting a tool's own docstring for whether it has been RUN** | `eval/movers.py` says "UNRUN, no number in this repo comes from it" - **stale**. Its primitives ran 2026-08-24 via `eval/candidate_audit.py --movers` and `eval/foot_gate_power.py` over 30 clips, and two STATE rows already carry the results. The docstring was never updated. Cost: the lead told the founder the idea was untested | [evidence/far-player-motion-contrast-hypothesis.md](evidence/far-player-motion-contrast-hypothesis.md) |
| **The horizon crop (`movers.crop_row`) at `k = 1.0`** | **safe but inert** | [evidence/the-horizon-crop-at-the-pre-registered.md](evidence/the-horizon-crop-at-the-pre-registered.md) |
| Court + vertical cone gate for false alarms | real far balls and fixtures overlap in court coords (real span **-229..+1667 m**) | - |
| Scaling the fixture radius 12 -> 18 px | halves false-fire (13.2 -> 5.7%) but costs **4.3 pts** far-court recall | - |
| Offline live-ball trajectory filter | net-negative once suppression runs; recall 50.2 -> **40.5%**. Retired | - |
| Detector fusion (TrackNet + WASB) | rescued **4 frames** and doubled the dominant cost | - |
| Dead-time "silence" negatives | the wrong negatives - confusers are not silence | - |
| Depth-aware Kalman process noise | median-referenced made false-fire **worse** (19 -> 27%) | - |
| Raising detector score threshold | 0.6 and 0.7 both **fail the recall gate** | - |
| Shrinking smoother `max_gap_s` | every value fails; solid ghosts sit at **9 regardless** | - |
| **Motion attention (TrackNetV4)** | conclusion stands on the right population: **59.2%** of false locks travel with a person, only 38.0% are static scenery | [evidence/motion-attention.md](evidence/motion-attention.md) |
| **Depth-invariant static-player guard** (`body_relative`) | **GATE FAILS on 1 of 3** calibrated clips | [evidence/depth-invariant-static-player-guard.md](evidence/depth-invariant-static-player-guard.md) |
| **`--pose-quality accurate` for the far player** | **MEASURED NEGATIVE**, gate pre-registered before the run | [evidence/pose-quality-accurate-for-the-far-player.md](evidence/pose-quality-accurate-for-the-far-player.md) |
| Pose-proximity negative mining | **11.4%** catch at the 5% collateral ceiling vs a 60% gate - a skeleton has no racquet (**2.12 body heights** away) | - |
| **Racquet-box negation (COCO class 38)** | **failed twice**; the second run found why - COCO finds the *near* player's racquet while the detector fires on the *far* player's | [evidence/racquet-box-negation.md](evidence/racquet-box-negation.md) |
| **Tightening it to the racket HEAD** | **the head is not the discriminator** | [evidence/tightening-it-to-the-racket-head.md](evidence/tightening-it-to-the-racket-head.md) |
| **Screening far-court gaps at SELECTION time** | **GATE FAILS under cross-validation**; 569 passing feature pairs cross-validate to **0-3%**. The shuffled-label null control returns **0** passing pairs, so the signal is real and far too weak | [evidence/screening-far-court-gaps-at-selection-time.md](evidence/screening-far-court-gaps-at-selection-time.md) |
| Raising `acquire_bound_m` 4 -> 10 m | +0.6 pt recall for +1 ghost | - |
| Blur augmentation alone | dead end on its own; only pays combined with occlusion work | - |
| **Retuning `max_gap_s` for 60 fps** | **GATE FAILS on replication** - clean on yt_rally2, collapses on am_hard_utr. The optimal gap policy scales with detection density; never tune it on one clip | [evidence/retuning-max-gap-s-for-60-fps.md](evidence/retuning-max-gap-s-for-60-fps.md) |
| **A second bounce HYPOTHESIS in the smoother** (`bounce_hypothesis`) | **GATE FAILS on P2 and P6**, measured over all 10 gold clips (1658 clicks, 272 no-ball frames). Recall PASSES (47.0 -> 48.1%, +18 hits) and **separation PASSES at 9.00:1 against a >7 bar** - so it DOES beat the structural exchange rate. It fails because ghosts rise on **5 of 10** clips and recall *falls* on `gold_UHf0LeMU2pg` (-3 hits, +5 wrong). Off by default. | [evidence/bounce-hypothesis.md](evidence/bounce-hypothesis.md) |
| **`bounce_hypothesis` v2 - the position fix** (`restitution_set`) | **GATE FAILS on 4 of 7 bars** over all 10 gold clips. P1 recall PASSES (47.0 -> 48.1%, +18), P4 separation PASSES (9.00:1), P5 PASSES; **P2** ghosts rise on 5 of 10, **P3** am_hard_utr 69 -> 73 (bar >=77) and yt_match40 124 -> 127 (bar >=132), **P6**, **P7** `wrong` rises on 4 of 10. The gate's NAMED cause is **disconfirmed**: v2's y-gate is strictly tighter than v1's and the rises survived. Off by default. | [evidence/bounce-hypothesis-v2-gate.md](evidence/bounce-hypothesis-v2-gate.md) |
| **The reflected hypothesis has its own FALSE-ACCEPTANCE region** | Measured 2026-08-29: at every `miss -> wrong` frame the raw detection was already 20-502 px from the human click, and the reflected model admitted it at the UNMODIFIED S. Falsifies the design claim that a ghost fits neither hypothesis - negating `vy` moves the prediction far enough to cover a lock **502 px** off the track. A second failure mode is segment-restart degradation (a 0.7 px raw detection emitted 11.3 px off). | [evidence/bounce-hypothesis-v2-gate.md](evidence/bounce-hypothesis-v2-gate.md) |
| **"More restitution hypotheses" is the WORSE half of v2** | Ablation, one variable: band removed at a SINGLE e=0.75 gives **+21 hits / +5 wrong / 10.50:1**, better than v1 (+18/+7/9.00:1) and better than v2's three-e set (+18/+6/9.00:1). Removing the variance inflation is the gain; enumerating e costs 3 hits. Neither clears P7. | [evidence/bounce-hypothesis-v2-gate.md](evidence/bounce-hypothesis-v2-gate.md) |
| **Bounce-aware smoother reset** (`bounce_reset`) | **FAILS gate on all 3 clips**; best case **1.4 pts short** of `real_landing` +5. Off by default | [evidence/bounce-reset.md](evidence/bounce-reset.md) |
| **Lowering the smoother's `reset_after`** | **GATE FAILS on replication** | [evidence/lowering-the-smoother-s-reset-after-to.md](evidence/lowering-the-smoother-s-reset-after-to.md) |
| **A burned-in SCOREBOARD as ground truth for points/rallies** | **BUILT, THEN REJECTED ON THE PREMISE** and reverted (`afffb5a`) - it is somebody's data entry, not the court. Do not rebuild | [evidence/using-a-burned-in-scoreboard-as-ground.md](evidence/using-a-burned-in-scoreboard-as-ground.md) |
| **Single-frame court auto-seed in the setup tool** | **7 of 10 worse than starting from a blank rectangle**; only multi-frame consensus separates the cases | [evidence/single-frame-court-auto-seed-in-the.md](evidence/single-frame-court-auto-seed-in-the.md) |
| **Lowering the court consensus bar 6/8 -> 5/8** | **GATE FAILS** - the one 5-vote clip is wrong by 68.7 px | [evidence/lowering-the-court-consensus-bar-6-8.md](evidence/lowering-the-court-consensus-bar-6-8.md) |
| **Raising the detector's input resolution** | **Gate B FAILS on both clips** | [evidence/raising-the-detector-s-input-resolution.md](evidence/raising-the-detector-s-input-resolution.md) |
| **Making the smoother respect suppression** (`blocked` mask) | **GATE FAILS on the recall guards** | [evidence/making-the-smoother-respect-suppression.md](evidence/making-the-smoother-respect-suppression.md) |
| **Tightening the smoother gap to cut ghosting** | 0.10 s halves ghost frames but the trade is ~1:1 against recall, and only for FADED ghosts | [evidence/tightening-the-smoother-gap-to-cut-ghosting.md](evidence/tightening-the-smoother-gap-to-cut-ghosting.md) |
| **Mining `suppress_false_locks`' rejections as hard negatives** | **GATE FAILS**, and it corrects an over-attribution | [evidence/mining-suppress-false-locks-rejections-as-hard.md](evidence/mining-suppress-false-locks-rejections-as-hard.md) |
| **Mining whole-frame hard negatives at all** | **Gate C fails**, and it names the root cause | [evidence/mining-whole-frame-hard-negatives-at-all.md](evidence/mining-whole-frame-hard-negatives-at-all.md) |
| **Localised confuser weighting** (Session I) | **PRODUCT GATE FAILS** - pooled solid ghosts **14 -> 15** while the detector improved on 6 of 6 clips. Unattributable: the trainer had no seed | [evidence/localised-confuser-weighting.md](evidence/localised-confuser-weighting.md) |
| **Expecting a detector gain of ANY kind to reach the product** | **four for four** | [evidence/expecting-a-detector-gain-of-any-kind.md](evidence/expecting-a-detector-gain-of-any-kind.md) |
| **Telling labellers the rule instead of enforcing it** | **MEASURED NEGATIVE** | [evidence/telling-labellers-the-rule-instead-of-enforcing.md](evidence/telling-labellers-the-rule-instead-of-enforcing.md) |
| **Filling far-court labels by interpolating between anchors** | **MEASURED NEGATIVE** - the anchors bracketing the gaps were themselves false locks | [evidence/filling-far-court-labels-by-interpolating-between.md](evidence/filling-far-court-labels-by-interpolating-between.md) |
| Improving CourtNet for auto-calibration | wrong target - CourtNet is **Tier 2**; `courtfit` consensus is Tier 1 and beats it | - |
| **Finding burned-in graphics by any temporal statistic** | **all three fail on this footage, in both directions** | [evidence/finding-burned-in-graphics-by-any-temporal.md](evidence/finding-burned-in-graphics-by-any-temporal.md) |
| **Screening far-court gaps by lock kinematics** | **two measured negatives** - and they are why the anchor control exists | [evidence/screening-far-court-gaps-by-lock-kinematics.md](evidence/screening-far-court-gaps-by-lock-kinematics.md) |
| **Downscaling the pose INPUT to afford it on an A13** (P0-2) | **NOT ESTABLISHED - the `yt_match40` column is WITHDRAWN 2026-08-28** (`11.0% @1280` was the NEAR player under a mislocated net line). Surviving evidence is `am_hard_utr` 1.0 -> 0.0 -> 0.0, which the file itself says cannot measure a 2-pt gate. Re-run after the clip is re-calibrated | [evidence/pose-downscale-far-player.md](evidence/pose-downscale-far-player.md) |
| **A precision boundary above where int8 erosion 'first appears'** | **The premise is FALSE - refuted 2026-09-04, and the control is what proves it.** Per-layer activation diff over the 36 Relu/BN tensors both graphs share by name, same real input. Relative L2 rises through the encoder, peaks at the 512-channel bottleneck (layer 19) and falls through the decoder - **identically on the failing frame and on two frames that decode correctly**: peaks **0.282 (fails) / 0.281 / 0.261 (agree)**, within **4%** across the whole encoder and **35% QUIETER than a control at the output**. **Quantisation error is not elevated on the failing frame at any layer**, so there is no boundary to install - raising precision anywhere would target a noise level identical on frames that work. Explains why Arm C recovered 1 px of area out of 13: not the wrong place, no place. **int8 is not the disease** - the exposure is the fp32 model's own ~5% top-2 blob margin, so ordinary noise flips an already-marginal decision. Points at a **top-2 margin refusal signal** (chain-side, protects fp32 too) rather than any quantisation flag. Third arm in this family; do not fund a fourth | [evidence/ball-detector-parity-tracknet.md](evidence/ball-detector-parity-tracknet.md) |
| **Net-tape camera-height cross-check: AGREE - and the alarm that prompted it was measurement noise** - 2026-09-05 | **Nothing. Answered.** The net tape gives a camera height that never touches the four clicked corners, so it cross-checks the fitted height independently. **AGREE: 13 of 15 clips within 10%**, n=15 of 27 (bar needed >=6), directions **8+ / 7-**, sign test p=1.0, median row offset +0.2 px - **no systematic bias**. **The three disagreements that prompted this did not survive an automated measurement**: `am_hard_utr` -12.8% -> **-3.7%**, `yt_match40` +12.2% -> **+6.7%**, `sAjkpeRq4P4` -33.3% -> **+5.4%**. **Why, and it is the finding: the instrument is precision-limited and nobody had priced it** - `dH/drow = H^2/(0.914*(ground-horizon))` is **3.2%/px at 720p**, so the whole 10% bar is **~3 px of tape row**; the -12.8% and the -3.7% are the same object measured **6 px apart**. **Not merely noise either:** four courts appear twice and **every pair agrees in sign and to <=2.6 pp**, so the residual spread is stable and court-specific - the signature of **net sag / non-regulation stringing**, not calibration error. Two outside the bar, both isolated: `demo30` +75.4% (47.9 px net span, below the instrument's resolution - unmeasurable) and `L73ep7JHiJ4` **-22.3%, the one genuine geometric disagreement** (z 11.6, band 20.9 px above projection). **Unresolved conflict, not smoothed over:** on `sAjkpeRq4P4` qa's hand profile puts the tape at row 406-409 and the matched filter at 437.8 - **~30 px apart**; the verdict holds either way (12/15 still clears 2/3) but that clip's row is conditional. **Next tiebreaker: the net POSTS at 1.07 m, which do not sag** - they share the camera-height unknown but not the sag unknown | [evidence/net-tape-camera-height-consistency.md](evidence/net-tape-camera-height-consistency.md) |
| **Net-anchor calibration check built; its two pre-registered bars FAILED** - 2026-09-05 | **A human eye on two clips, and a tape/post DETECTOR before any bar.** Projects features never clicked - net line, net posts - so it is non-circular by construction, and reports `horizon_row / net_ground_row / net_tape_row` at their REAL heights (`NET_POST_OFFSET` 0.914, `NET_HEIGHT_CENTER` 0.914, `NET_HEIGHT_POST` 1.07, plus singles sticks, mirrored to JS and parity-tested). **27 of 29 calibrations now carry an independent net-anchor render.** **Both pre-registered bars FAIL and were NOT moved:** `band_ratio<1.5` and `|dy| > 0.5x net px height` flag **14 of 27** (four of them PASS-stamped) and **INVERT on the only pair with settled truth** - the CORRECT `yt_match40` flags at 0.78 while the known-WRONG `.bak` passes at 7.84. Retained as reported numbers with the failure stated in code, CLI and doc. The one quantity that does separate the pair (`horizon_row` -382 at hfov 20.7 deg) is **not independent** - it is the existing camera-height screen re-expressed from the same four corners, so no bar is proposed on it. **The two unsettled sheets are NOT resolved and the 'far corners near the NET' reading is unsupported**: `sAjkpeRq4P4` is not flagged at all (ratio 5.64, dy -2) and `am_hard_utr` is internally consistent to 0.4 px (ratio 4.36). **qa-verified 2026-09-05 and the lead's frame-read CORRECTED: neither clip is settled, and `sAjkpeRq4P4` is the WORSE of the two, not the safer.** qa refused to eyeball and instead measured the real tape row by mean-brightness profile on a clean frame over disjoint column ranges: `am_hard_utr` tape offset **~8-9 px** with the **ground line an exact match**; `sAjkpeRq4P4` tape offset **~29-31 px** AND ground offset **~22-25 px**, both the same direction - despite the automated bar reading it clean. The lead had read `sAjkpeRq4P4` as clearly correct by eye; that is the third frame-read error of the day and is why both stay open | [evidence/net-anchor-calibration-check.md](evidence/net-anchor-calibration-check.md) |
| **Court AUTO-detection is CLOSED for v1 - manual calibration is the PRODUCT ANSWER, not a fallback** - assessed 2026-09-05 | **Nothing. Concluded.** No line-based branch, classical or learned, better fitter or better search, can be shown to reach the shipped **8.1 px** bar. **The reason it is a ceiling and not a defect:** the line detector's ~**6.4 px** disagreement with truth is the same order as the human corner-click neighbourhood itself (~**5.8 px**), so much of it is plausibly label/definitional noise - paint width, blur, click spread - with nowhere obvious to improve to. CourtNet keypoints and dense segmentation were both weighed and rejected on EXISTING evidence, not re-run: CourtNet improvement is already closed in this table, and segmentation attacks coverage/fire-rate, not the precision ceiling that actually binds. **The iOS constraints disqualify nothing** - the case against these is accuracy, not exportability; a learned net would in fact be *easier* to ship than the 2,900-line classical pipeline, which has no iOS conversion path at all. **This frees that whole mobile port**, now for an accuracy reason rather than only engineering cost. **The single falsifier, cheap and NOT yet run:** click points *along* each court line (not just the 4 corners) on a handful of gold frames and measure the detector against that direct truth - **>10 px** means real fixable bias and justifies a narrow reopening; **~5-7 px** corroborates the ceiling as near irreducible | [evidence/court-detection-path-after-the-line-ceiling.md](evidence/court-detection-path-after-the-line-ceiling.md) |
| **Least-squares over ALL matched line correspondences, to beat the 4-point fit ceiling** | **FAILS the pre-registered bar 2026-09-04, and the branch dies with it.** Control first, and it is exact: the 4-point fit recomputed in the same run gives **17.10 px@640** with an **identical survivor set** and **0.00 px** max per-clip difference from the committed `corr_attrib.json` - it is the 2026-08-29 construction line for line. Against a <=10.0 px bar: **LS-geom 19.80 px, LS-DLT 73.50 px**. Both pools fail (TUNE 19.0->23.5, SHELL 6.4->6.8), better on 7 clips and worse on 5, paired Wilcoxon **p=0.97** - no direction at all. **The ceiling is one stage UPSTREAM of the fit:** LS-geom drives the line residual below the exact fit on **13 of 13** and below the **human homography** on **13 of 13** (3.01 vs 6.44 px rms). The optimiser works perfectly; the **detected lines** disagree with the true court by ~6.4 px, so a better fitter just converges harder onto a biased target. **This kills the solver's other two failures too** - C6's 12.6x cost and the 22-of-30 die-before-scoring are SEARCH problems, and this run handed the search a PERFECT answer and still landed 17-20 px against the shipped 8.1. **No search improvement reaches the bar.** One discarded-not-tuned method note: projecting world endpoints explodes on low mounts (204 px under the *human* homography on `hillsborough_p02`, far baseline at the vanishing line) - an objective the truth does not minimise cannot test anything | [evidence/least-squares-court-fit.md](evidence/least-squares-court-fit.md) |
| **Re-admitting the smoother's gate rejections using the BACKWARD (RTS) pass** | **FAILS the pre-registered bar, 2026-09-03 - 0 of 3 clips, 2 of 3 required.** The premise was live (the RTS loop never re-reads `positions[i]` nor writes `used[]`, so it does not already re-admit) but the signal is at chance: best real-to-ghost ratio **1.14 / 2.00 / 0.56 : 1** against a >=3:1 bar, pooled **0.93:1** on 21 real vs 28 ghost rejections, and the mandatory shuffled-label null control is reached or beaten by **526 of 1000** seeded permutations. **The nearest reject to the smoothed track is a GHOST on 2 of 3 clips.** Mechanism, and it is a confound not a tuning problem: a ghost continuing the model's stale motion sits ON the smoothed path, while a real detection fails a 99.9% chi-square gate essentially only when the model is stale - and RTS is blocked at segment boundaries, so the backward pass smooths that frame off **the same stale model**. It is a second look at the evidence the forward gate already got wrong. **Any future re-admit signal must come from OUTSIDE the motion model** - appearance, detector confidence, cross-detector agreement. Also found: the shipped code already re-admits the `rej >= reset_after` re-seed (1/19, 3/16, 8/26 of rejections), excluded here so nothing claims credit for shipped behaviour. Power is thin (18/13/18 adjudicated rejects, capped by needing a human-clicked frame) and stated, not buried - but the pooled set fails too and the failure is a wrong ORDERING at the top of the ranking, not absent signal. **Third measured negative in the smoother-gate family; per rule 3 do not propose a fourth** - including the cross-detector variant, which the mechanism above makes tempting and which this row bars | [evidence/smoother-gate-backward-readmit-separation.md](evidence/smoother-gate-backward-readmit-separation.md) |
| **Per-channel int8 (`per_channel=True`) to stop the ball graph's blob-area erosion** | **REJECTED 2026-09-03 - it is a silent NO-OP, not a loss.** The graph is byte-identical to the shipped one (same sha256, same 10,918,923 bytes): `quantize_dynamic` forces `IntegerOps`, mapping Conv to `ConvInteger`, and ORT's `ConvInteger` class has **no per-channel branch** - only `QLinearConv` and `QDQConv` read `is_per_channel()`. TrackNet is 18 Convs, so the flag touched zero weights. Reaching real per-channel needs static QDQ + a calibration set = a different experiment | [evidence/ball-detector-parity-tracknet.md](evidence/ball-detector-parity-tracknet.md) |
| **Keeping the FINAL conv in fp32 (`nodes_to_exclude`) to stop the same erosion** | **REJECTED 2026-09-03**, 3 of 4 screen frames still fail; the one it fixes is the exact-tie frame. A genuine graph change (17 `ConvInteger` + 1 fp32 Conv, +0.44 MB), so this is a real negative: the true blob's area went **15 -> 2 (shipped) -> 3 (Arm C)** against a target of 15, and one frame got 0.16 px *worse*. **The erosion is already in the int8 features ARRIVING at the final conv** - output-layer precision is the wrong lever, and any third mitigation must act upstream | [evidence/ball-detector-parity-tracknet.md](evidence/ball-detector-parity-tracknet.md) |

---

## Withdrawn figures

Numbers this project published and later retracted. **This table is machine-read:**
`.claude/hooks/withdrawn-guard.sh` refuses any commit where one of these strings
still appears in a live doc without a withdrawal marker in the same block. Add a row
the moment you withdraw a number — that is what stops a stale copy surviving
somewhere else, which has now happened three times.

| Figure | What it claimed | Withdrawn because | Date |
|---|---|---|---|
| `1.47x` | rally segmentation over-split vs real points | read off a burned-in scoreboard; the user rejected that premise and the tool was reverted (`afffb5a`) | 2026-08-15 |
| `1.6x` | the same over-split, re-counted | same source, same rejected premise — it should have gone with the 1.47x and did not, surviving in the Open table and in Trap T20 | 2026-08-17 |
| `1.6×` | as above, unicode-multiplication-sign spelling | registered separately because a literal-string check cannot see it otherwise | 2026-08-17 |
| `4.50:1` | the `bounce_hypothesis` separation ratio, said to be below the >7 bar and therefore worse than the structural exchange rate | a two-event denominator; re-measured over all 10 gold clips (272 no-ball frames) it is **9.00:1**, which passes | 2026-08-27 |
| `4.50 : 1` | as above, spaced spelling | registered separately because a literal-string check cannot see it otherwise | 2026-08-27 |
| `0.18–0.31` | the true court's agreement score, said to fall below the 0.33 accept gate on 5 of 10 calibrated clips — cited as proof that "the criteria reject the correct answer even when handed it" | it scored the human's four clicked corners **exactly**, but the gate defines correct as anything within 20 px @640. A court a median **5.8 px** from the clicks clears the gate on **9 of 10** clips, not 5 (`eval/truth_neighbourhood.py`). It measured our labelling, not the criteria — and it had already gone out in an external research brief, which ranked its recommendations on the strength of it | 2026-08-24 |
| `0.18-0.31` | as above, ASCII-hyphen spelling | registered separately because a literal-string check cannot see it otherwise | 2026-08-24 |
| `11.0% @1280` | the far-player pose detection rate on `yt_match40` that P0-2's gate turned on | `data/yt_match40_pts.json` is miscalibrated - all four clicked corners are off any court line - so the pipeline labelled the NEAR player FAR. `far_kpts` non-null on 1125/10268 frames IS 11.0%, but of the wrong person | 2026-08-28 |
| `8.8 m mount` | the camera height/geometry of `yt_match40`, used to describe it as broadcast-style footage | same bad homography; from the pixels the clip is ~5.4 m behind the baseline and under ~2.2 m high - a tripod, the same class as `am_hard_utr` | 2026-08-28 |

Not covered here on purpose: `docs/archive/HANDOFF.md`, `docs/archive/sessions/`, `docs/REVIEW-*` and
`data/output/*` are point-in-time records. They are SUPPOSED to still contain the old
number — that is what a dated record is. The guard skips them.

---

## Open, and what each is waiting on

| Item | Waiting on | Evidence |
|---|---|---|
| **The SHIPPED int8 ball graph fails its parity bar on **5 of 9** informative gold clips, and ALL THREE mitigations are spent** - widened to the FULL 10-clip gold set 2026-09-05; qa-verified; third arm measured out 2026-09-04 | **A product call: ship int8 with a ~1-in-100 catastrophic-lock rate, ship fp32 at 4x the size, or fund a third mitigation.** Six clips x 178 contiguous frames, int8 vs fp32 both through the real JS `_decode()`. **FAIL** `am_hard_utr` 70.8 px, `yt_rally2` 75.4 px (3 CONSECUTIVE frames), `gold_shell` **185.1 px** - the worst measured anywhere, on the clip with the BEST null agreement (99.4%) and a 0.000 px median. **PASS** `yt_match40`, `gold_clay`, `gold_am` (max 1.4 / 1.0 / 0.7 px). Conditions 1-2 pass on all six without exception; the no-frame-over-10px condition is the only one that has ever fired. Pooled **5 failing frames / 528 both-fire = 0.95%**, **3 of 6 clips**. Mechanism confirmed on every reject inspected: quantisation moves blob AREA, never the peak. **Arm B `per_channel=True` REJECTED - the graph is BYTE-IDENTICAL to shipped** (ORT's `ConvInteger` has no per-channel branch; all 18 weight scales stayed scalar), so per-channel is unreachable via `quantize_dynamic`. **Arm C `nodes_to_exclude=[final Conv]` REJECTED** - a real graph (17 ConvInteger + 1 fp32 Conv, +0.44 MB) that still fails 3 of 4 screen frames and recovers the true blob's area only **15 -> 2 -> 3**; the negative localises the fault UPSTREAM of the output layer. qa independently recomputed the pooled numbers (confirmed exactly) and corrected the close-race explanation: *zero close races on both cleanly-passing clips* survives every threshold 0.05-0.30, but *all five failures are close races* drops to 2 of 5 at 0.05, so no flip-rate is quotable | [evidence/ball-detector-parity-tracknet.md](evidence/ball-detector-parity-tracknet.md), [evidence/int8-parity-qa-verification.md](evidence/int8-parity-qa-verification.md) |
| **The shipped speed gate `seen_frac >= 0.5` is WEAKLY predictive and cannot be rescued by re-tuning** - measured 2026-09-03/04, qa-verified with a positive control | **A replacement GATE, not a replacement threshold.** `pipeline.py:1873` gates every speed shown and had never been tested. Accept-precision margin over base rate **+4.96 / +3.11 pts against a >=10 bar, 0 of 10 seeds** (FAIL). The held-out sweep then found **the acceptance test itself defective** (satisfied by refusing 81% of shots), and the repaired bar returns **NONE ADMISSIBLE for any coverage floor above 27%**. Court-coverage disqualifies itself as mechanically confounded. **Two earlier claims in this row were WITHDRAWN** (a 4.0 m runoff defect): the gate is not at chance, and it refuses the *worse* shots, not the better ones. **No threshold named** | [evidence/does-seen-frac-predict-speed-error.md](evidence/does-seen-frac-predict-speed-error.md), [evidence/seen-frac-gate-qa-verification.md](evidence/seen-frac-gate-qa-verification.md) |
| **The point-boundary label protocol EXISTS - the score layer's ground truth is unblocked** - written 2026-09-03 | **The founder's ~3-6 h labelling session, which was gated on exactly this.** Rule 12 put the score layer back in scope but created no ground truth, and rule 11 bars the easy source. The protocol fits the budget at **~5.6 h worst case** - and only because the labelling hard-stop was set to 4.5 h rather than the priced range's own top end, so **the cut is already made in the document** (self-agreement segment first, never the edit-scrub prerequisite). Boundary defined as first-serve racket contact to a **5-reason end enum**, with a REFUSAL path on every ambiguous case (let, second serve, no-decision rally, off-screen gap, stoppage) - because a written-but-unenforced rule already measured worse labels here once. 11 point-level fields, each tagged to the consumer that needs it; dead time deliberately NOT a field (derived arithmetic). Gold goes to `data/gold/<clip>.points.json`, deliberately NOT `schema.py`'s `Rally` - reusing the product contract would let gold masquerade as computed output. Guard named `assert_no_point_boundary_gold_leak`, keyed on basename per the ball guard's precedent (T17). **Cross-guard finding: 4 of the 9 eligible raw files are ALREADY ball gold under the same basename** (`L73ep7JHiJ4`, `sAjkpeRq4P4`, `UHf0LeMU2pg`, `uR5q2cSM6AY`) - different task, same match, so a future point-boundary heuristic could be tuned and evaluated across both without anyone noticing. Verification is self-agreement only (no second labeller exists): a fixed 15-20 min blind re-label against a 1.0 s median-disagreement bar. **300-600 points supports point count and tolerance-based alignment; it does NOT support rare-event rates** | [evidence/point-boundary-label-protocol.md](evidence/point-boundary-label-protocol.md) |
| **Top-2 blob margin as a REFUSAL signal: PASSES its screen on fp32, FAILS on int8** - measured 2026-09-04 | **A wider run before anyone calls it a detector.** Comes from the activation diff: int8 is not the disease, the exposure is the fp32 model's own close two-blob race, and the failure's defining property is a confident wrong lock with **no refusal signal**. Margin = `1 - score_2/score_1` over `area x peak`; both blobs are already computed, so the cost is a comparison. **fp32: catch 5/5 bad frames at 2.1% collateral** (`t=0.10`; any `t` in [0.077, 0.30] is identical, so the threshold is not load-bearing - and `t=0.077` was EXCLUDED though it passes, being exactly the widest bad-frame margin, a threshold drawn round the numerator). Null control separates hard: **three** nulls including a selection-adjusted one and a cluster-preserving circular shift, all p<=0.001; exact hypergeometric 1.3e-08. **AMENDED 2026-09-04: int8 CAN police itself, weakly.** The original "int8 fails at every threshold" was searched on a grid stopping at **t<=0.30**, inherited from the fp32 sweep - and one bad frame's int8 margin is **0.86**, above the top of the grid looked at. On a wider sweep `margin_int8 <= 0.90` catches **4/5 at 3.82% collateral, 16.7% precision**, and the equivalent int8-only rule **`blob count >= 2`** catches **4/5 at 4.78%, 13.8%** - the two refuse the identical 29 frames, so they are one rule: what is informative on int8 is that a runner-up blob **exists**, not that the race is close. Nulls separate (hypergeometric 3.6e-5; selection-adjusted over the 108-rule grid p=0.0000; cluster-preserving p=0.0010). **"Small winner" is REFUTED** - on every bad frame int8's winner is ordinary (area 12-13 vs median 12, peak 242 = the max AND mode); catching 4/5 on area alone needs refusing **81.6%**. So the fp32-only claim was too strong: fp32 is a **closeness** test (5/5, 31% precision), int8 a **presence** test (4/5, 14-17%) that **misses the single-blob collapse** it would most want to catch. **THE QUALIFICATION IS THE RESULT: refusal precision is 5/16 = 31%**, and at identical margin *including an exact 0.0000 tie* the decode is right **3 times in 4**. So what is established is only *every int8 failure is a close race, and close races are rare* (37/528 frames have a second blob) - **NOT that a low margin predicts failure**. A conservative risk gate discarding ~2 correct answers per error avoided. Effective n is **~3** (`yt_rally2` 0108-0110 is one event) and the verdict rests on a 2.7-point-wide step - one frame's luck from flipping | [evidence/top2-margin-refusal-signal.md](evidence/top2-margin-refusal-signal.md) |
| **`verify_court`'s coverage gate does not separate correct courts from wrong ones** - measured 2026-09-04 | **Nothing. Unblocked.** Shipped gate run over all **25 committed human calibrations with video**, `H` from the four human clicks. **3 of 25 correct courts refused**; the `visible>=0.30` gate is **inert** (range 0.927-1.000). The finding is that the 0.40 bar sits **inside** the correct-court distribution (0.245-1.000) and **`yt_match40`, the grossly-wrong T23 clip, PASSES at 0.436 above two correct courts** - coverage orders clips by line VISIBILITY, not correctness. **Do not retune 0.40.** A camera-height screen was proposed and then **CORRECTED same day to a MOUNT-TYPE test, not a correctness test** - it would false-reject `eala_pts_auto`, a correct Wimbledon broadcast calibration. **No threshold proposed.** A same-day claim that a WRONG court scored 0.944 coverage is **WITHDRAWN** - the calibration was correct and the lead had compared the projected net GROUND line against the net's TOP TAPE, 0.914 m above it | [evidence/verify-court-false-rejects.md](evidence/verify-court-false-rejects.md) |
| **Far-player CONTRAST is CHARACTERISED but has NO GATE** - measured 2026-08-29 | **A pre-registered bar, if anyone ever wants a verdict.** Nobody had ever measured this, so there was nothing to pre-register and inventing a bar now would violate rule 2. Descriptive only, n=15 POST-HOC far-player boxes on `yt_match40`, CIELAB against the surrounding court: \|dL\| median **5.96** (0.11-13.30), dChroma median **11.71** (6.04-25.28), dE median 14.67. **\|dL\| never reaches the surround's own SD of L\* on any frame** (ratio median 0.30, max 0.59), so colour is the stronger channel. Contrast does not separate the frames motion found from the ones it missed. The motion half of this question is answered and filed above | [evidence/far-player-motion-gate-result.md](evidence/far-player-motion-gate-result.md) |
| **Far player vs. far ball: NOT the same problem** - established 2026-08-29 | **The lead, to resolve a contradiction in the proposal's own gate.** The player is SEARCH-limited (full-frame pose 0/25; crop+upscale 2/25 strict, 15/25 post-hoc) and the ball is DISCRIMINATION-limited (detector already fires on 73-76% of far-court frames). The ball's own analog of the player's fix - a whole-frame resolution bump - was already run and its **entire gain arrived as extra solid ghosts**, one of the four-for-four closed detector items. All 19 chain false locks carry **`run_len = 1`**, a real ball's own kinematic signature, which is why nine chain attempts testing for non-ball-like behaviour all failed. **BLOCKING FLAW in the top proposal's gate:** it centres the crop on "the far service-box/baseline band's **projected** image position" while also declaring itself "homography-free by construction" - projection needs a homography, and `yt_match40`'s is void (T23). Same failure as P0-3's original unexecutable test. Not dispatchable until the prior is redefined or the corners are re-clicked | [evidence/far-end-player-and-ball-what-is-left.md](evidence/far-end-player-and-ball-what-is-left.md) |
| **v1 ball detector: TRACKNET** - founder decision 2026-08-29 | Nothing. The chain test was SPLIT, so this was a product call, not a measurement: TrackNet's failure mode (fewer phantom balls, -8 real hits) was chosen over BallNet's, and TrackNet is the only one with a Core ML path today. **BallNet v21 stays the upgrade path**; its Core ML conversion is a scoped line item, not a footnote. The founder has an improvement idea for TrackNet still to be shared. **The coverage side of that trade is now quantified (2026-09-02, matched `detector_ab/` caches):** shots clearing the 50% `seen_frac` bar after the full chain are `am_hard_utr` **73 (BallNet) vs 50 (TrackNet)** and `yt_match40` **138 vs 103**. The gap is present at the RAW tracker row (77.7% vs 69.6%, 81.9% vs 68.8%), so it is the detector, not the chain. This is the `seen_frac` coverage bar, NOT `speed_confident` - a different, stricter test | [evidence/ballnet-v21-vs-tracknet-at-the-chain.md](evidence/ballnet-v21-vs-tracknet-at-the-chain.md) |
| **Line-call refusal band: DEFERRED** - founder decision 2026-08-29 | The founder, not a measurement. Line calling is parked; the 0.15 m / 0.20 m choice is NOT made. qa's margin curve stands and is unfiled: both real mounts are at or below the majority-class floor within 0.10 m of a line, clearing it from ~0.20 m; refusal costs 29.5% of close calls at 0.15 m and 39.0% at 0.20 m. `live.py`'s shipped `line_margin_m = 0.05` sits inside the unreliable zone and is UNCHANGED | - |
| **P0-3's substituted identity test: ACCEPTED** - founder decision 2026-08-29 | Nothing. The pre-registered test routed through `yt_match40`'s broken homography and was unexecutable; backend-dev substituted a calibration-free person-specific test and flagged the swap rather than making it quietly. Accepted | [evidence/p0-3-crop-around-contact.md](evidence/p0-3-crop-around-contact.md) |
| **Joint line-to-model correspondence - BUILT AND MEASURED 2026-08-29; THREE FAILURES** | **Nothing - it is measured, not blocked.** Step 1 (concurrency pencil grouping) WORKS: 7/8 held-out shell. The solver was then built and it fails three ways. **C6 cost: 12.6x** against a 5x cap. **C3 would fail even given the correct correspondence**: handed the true assignment, the reconstruction is a median **17.1 px@640** vs the shipped 8.1 px, because a homography from exactly four line intersections amplifies each line's error where the court is most foreshortened. And **22 of 30 tuning clips die before scoring** - after removing two screens that were my own error (size floors on the construction quad; a convexity test that is wrong everywhere, since a low mount puts the far baseline on the vanishing line and the correct image of a court is then NON-convex), the remaining kills are SPREAD across shipped criteria rejecting a correctly-labelled court - `verify_court` 9, pose 5, pencil supply 4, cam_refine 2. No single change rescues it. **Both continuations are now MEASURED and both fail - see the two rows above:** least-squares over ALL matched line correspondences instead of an exact 4-point fit (aimed at the 17.1 px ceiling), and why `verify_court` rejects a correct court on 9 of 30. | [evidence/court-correspondence-gate.md](evidence/court-correspondence-gate.md) |
| **Court detection: frames that find the RIGHT court disagree about its WIDTH** | Nothing. Unblocked and unaddressed. Subordinate to the search problem | [evidence/court-detection-frames-that-each-find-the.md](evidence/court-detection-frames-that-each-find-the.md) |
| **Indoor shell courts** - ground truth exists; the failure is the SEARCH | Nothing. Unblocked. 10 human calibrations arrived and are good | [evidence/indoor-shell-courts.md](evidence/indoor-shell-courts.md) |
| **`AGREE_PX` is 6x tighter on 4K than on the gate** | The wrong-court / search problem - it cannot ship before that | [evidence/agree-px-is-6-tighter-on-4k.md](evidence/agree-px-is-6-tighter-on-4k.md) |
| **Far-court recall** (detector fires on nothing in 24-27% of frames) | Human far-court labels: **4,087 frames**, 4-5 hours of clicking. Automating the selection is a measured dead end | [evidence/far-court-recall.md](evidence/far-court-recall.md) |
| **9 solid ghost balls, and why nothing removes them** | A better detector. All 19 chain false locks have `run_len = 1` - every survivor carries a real ball's kinematic signature | [evidence/9-solid-ghost-balls.md](evidence/9-solid-ghost-balls.md) |
| **Confirming the localised-weighting detector win** | ~2h20m of GPU | [evidence/confirming-the-localised-weighting-detector-win.md](evidence/confirming-the-localised-weighting-detector-win.md) |
| **Whether more data is what actually caused the recall gain** | **n = 1 training run per arm**; the datasets differ in size so batch composition differs too | [evidence/whether-more-data-is-what-actually-caused.md](evidence/whether-more-data-is-what-actually-caused.md) |
| **Whether a better detector can reach the ghost ball at all** | **ANSWERED IN PART 2026-08-28: yes, but only a model SWAP.** Four PARAMETER-level gains reached nothing; swapping BallNet v21 for TrackNet moves solid ghosts **88 -> 62** at flat recall. Still open: which stage absorbs a *retune*, and whether the ghost win survives the product half (it did not, on 2 clips) | [evidence/ballnet-v21-vs-tracknet-at-the-chain.md](evidence/ballnet-v21-vs-tracknet-at-the-chain.md) |
| **Off-machine backup** | Second disk verified 2026-08-17; off-machine still open | [evidence/off-machine-backup.md](evidence/off-machine-backup.md) |
| **The far player is a DETECTION problem on the target footage** | Settled 2026-08-17; `--far-player-rescue` recovers frames the shipped guard then deletes | [evidence/the-far-player-is-a-detection-problem.md](evidence/the-far-player-is-a-detection-problem.md) |
| **Bounce detection** | No true ball height from one camera. Audio impact is no longer unevaluated - see the audio screen row below; still unwired, and monocular 3D unevaluated | - |
| **Audio impact on indoor SHELL - SCREENED, not measured** - run 2026-08-28 | A per-stroke reference that rule 11 permits (human-clicked impact times, or boundaries from bounces and physics). **No recall or precision figure exists or may be derived** - the HUD scorer is barred for this. The screen is label-free and settles only the catastrophic case: `detect_impacts` self-declares useless on **0 of 88** clips over 309.3 min, including **0 of 62** indoor shell. Yield and contrast on the quiet founder venues are a third of outdoor, and the binding threshold is the level-dependent absolute one. Corpus census also corrects a stale repo claim that every clip is video-only | [evidence/audio-impact-feasibility-screen.md](evidence/audio-impact-feasibility-screen.md) |
| ~~**`detect_impacts`'s rolling floor allocates O(n x win)**~~ - **FIXED 2026-08-29** | **Done.** The deviation block is now evaluated in chunks bounded by `_FLOOR_CHUNK_ELEMS`. Measured at n=200k, win=1000: peak allocation **3.205 GB -> 0.131 GB (24x)** and **4.82 s -> 4.20 s**, **bit-identical** output. A 28.2 min clip is ~8.5x that n. Pinned by `tests/test_audio_floor_chunking.py`, which compares against the un-chunked expression directly at four window sizes and three chunk sizes - including one small enough to force many slices and one large enough to degenerate to the original single pass. **The iOS question is separate and still open**: Accelerate has no rolling-median primitive, so the port needs the streaming order statistic in `tools/audio_ondevice_probe.streaming_med_mad` (exact against numpy, pinned by `test_audio_streaming_floor.py`) - which is slower than vectorised numpy and so deliberately NOT used on desktop. | [evidence/audio-impact-feasibility-screen.md](evidence/audio-impact-feasibility-screen.md) |
| **`gate_ball_to_court` is NOT dead code - but it costs NO human-labelled ball** - measured 2026-08-29 | **Nothing. Answered.** It does remove locks: **4 of 7** calibrated gold clips - gold_sAjkpeRq4P4 **-674**, gold_uR5q2cSM6AY -43, gold_L73ep7JHiJ4 -30, yt_match40 -5 - correcting a stale "0 locks removed" claim that was measured on the detector-A/B caches and does not generalise. **But inspecting the REJECTS (rule 10) against human gold clicks, the shipped rung retains 100.0% of all 1,267 labelled ball frames and 100.0% of the 630 far-band ones, on all 7 clips.** So the removals land on frames no human labelled, and the gate buys precision without spending recall - the trade this project refuses. Rung A also beats the alternatives it replaced: pooled 100.0% vs 99.5% (rung B) and 99.1% (old), and on `am_hard_utr`'s far band **100.0% vs 53.8% vs 15.4%**. It still runs BEFORE the smoother, so smoother A/B deltas stay H-clean. `tools/eval_court_gate.py`. | [evidence/bounce-hypothesis-v2-gate.md](evidence/bounce-hypothesis-v2-gate.md) |
| **`smooth_forecast`'s ghosts are a PAIRING property, not the stage** - completed 2026-09-02 | **Nothing. Answered on all 7 calibrated clips, one variable changed.** Under **BallNet**: mean recall **-1.0 pts**, **+12** ghost fires, up on 3 of 7. Under **TrackNet** - the founder's v1 detector: mean recall **+3.9 pts**, **+3** ghosts, up on **6 of 7**. Same code, same clips, same human gold. So the stage needs no attention under v1's actual detector, and the earlier "adds ghosts on every clip" headline was true of the pairing it was measured on, not of `smooth_forecast`. Does NOT explain the -12.0 pt speed-coverage cost - different metric (`seen_frac` over hit->landing spans); both stand, and **that one was re-measured under TrackNet on 2026-09-02 and SURVIVES** (-11.0 / -8.1 pts), so the pairing verdict here is specific to the per-frame metric and does not generalise to the stage. **The ladder could not run TrackNet at all until 2026-09-02** (hardcoded BallNet, died on `KeyError: 'model_state_dict'`), so the project's own chain harness had never measured the v1 detector. | [evidence/smooth-forecast-adds-ghosts.md](evidence/smooth-forecast-adds-ghosts.md) |
| **Speed coverage is CHAIN-shaped, and the two stages are named** - the best-measured open target, **re-confirmed under TrackNet 2026-09-02** | Nothing. `smooth_forecast` costs **-11.0 / -8.1 pts under TrackNet** (BallNet, matched caches: -10.1 / -10.2); pre-registered pairing bar `|D_TN| <= 0.5|D_BN|` **FAILS**, stage bar `>= 0.75` **PASSES** on both clips, and it is still the largest single-stage cost (suppression -5.2 / -4.4). **The cost is a property of the STAGE, not the detector pairing** - unlike its ghost behaviour - because the innovation gate deletes **14-17%** of surviving real detections in every arm. Cross-checked on identical shot populations. Live-target floor met, so this stays the target it is recorded as. `gate_ball_to_court` costs exactly zero on 3 of 4 arms and -0.05 pts on the fourth. | [evidence/speed-coverage-is-chain-shaped-and-the.md](evidence/speed-coverage-is-chain-shaped-and-the.md) |
| **Ball-chain work is NOT closed — the stopping rule did not fire** | **Nothing. Unblocked.** The rule triggers if a separating mechanism lands at or below ~7:1; at full power `bounce_hypothesis` lands at **9.00:1**, so the premise that the exchange rate is a property of the SIGNAL is disproved. A second hypothesis beats it. **Updated 2026-08-29:** v2 tested the named position bug and the stopping rule STILL does not fire (its antecedent needs the `wrong` regression FIXED; v2 only reduced it, +7 -> +6). But "a fixable position problem" is no longer the right description - the reflected model has a measured false-acceptance region of its own, so a sixth attempt must constrain WHERE the reflected state may be adopted, not how `e` is chosen. | [evidence/bounce-hypothesis.md](evidence/bounce-hypothesis.md) |
| **8 court gold frames are mislabelled** | A minute of human re-labelling in the Lab. Deliberately not quietly edited | [evidence/8-court-gold-frames-are-mislabelled.md](evidence/8-court-gold-frames-are-mislabelled.md) |
| **Trimming was the missing first step** | Nothing - `tools/trim_clips.py` exists | [evidence/trimming-was-the-missing-first-step.md](evidence/trimming-was-the-missing-first-step.md) |
| **Core ML export requires macOS** (`coreml-export-requires-macos`) - found 2026-08-27 running P0-0 | A Mac to run `tools/export_coreml_p0.py` on. `coremltools`'s Windows wheel is pure Python; the native libs that serialize an `mlprogram`'s weights (`BlobWriter`) are absent, so export fails, not just the on-device measurement | [evidence/p0-0-coreml-export.md](evidence/p0-0-coreml-export.md) |
| ~~**A stamped calibration broke `run.py live` and 4 other entry points**~~ - found and FIXED 2026-09-02; **mobile live-call parity now VERIFIED WITHOUT VIDEO 2026-09-02** | **Both fixed.** `homography_from_landmarks` raised `KeyError: '_audit'` on any calibration carrying a `_`-prefixed marker - which is every file `validate_new_clip.py --stamp` or Court Setup (shape-lock off) writes, and 10 more were created on 2026-08-29. `pipeline.calibrate_video` had always stripped those keys; **the five other callers passed `json.load(f)` in raw** - `run.py`, `live_demo.py`, `show_in_action.py`, `build_court_dataset.py`, `calibrate.py`. Trap T15's shape: the filter lived in one caller and five were missing it. Now filtered inside the shared function, so a non-metadata unknown key still raises loudly. Pinned by `tests/test_calibration_metadata_keys.py`, which also loads every committed `*_pts*.json`. Found by running `mobile/verify_live.js`, which was itself dead on the same cause. **The mobile live-call parity expectation, then unverifiable, is now verified WITHOUT the missing video**: `LiveAnalyzer.push_position` (`backend/swingvision/live.py`) is pure - no frame, no cv2 - so `backend/live_replay_novideo.py` (new) drives it directly over the cached `ball_px` track the JS harness already uses, with the frame-count assumption cross-checked (not assumed) against `real_match.json`'s recorded fps/duration. Separately, `court_pts.json` (which the harness read) is stamped `DEGENERATE` (38.1px residual) by the project's own audit tool; `court_pts_refined.json` is "the good version of the same clip" per commit `20a672e` - a harness bug, not a genuine ambiguity. Fixed, both sides now agree **7 calls, 7 in / 0 out**, every `t_s`/`xy`/`margin_m` matching to 0.000 m against a pre-registered 0.001 m bar. **Not covered: the doubles branch** (known bug at `live_calls.js:145` - `isInSingles` ignores `this.singles`) and **any OUT call** - this track produced none under the correct calibration. | [evidence/live-call-parity-verified-without-video.md](evidence/live-call-parity-verified-without-video.md) |
| **Mobile viability is SPLIT, not uniform** - audited 2026-08-27 | Nothing. Unblocked. Live line calls are a straightforward port and largely done; the offline analyzer is a **rebuild** - its Kalman+RTS smoother is non-causal by construction and it runs whole-video passes. No Windows paths, no highgui, and every cv2 symbol used exists in the mobile builds. Two defects found en route: ~~mobile bundles a TrackNet export while the shipped default is BallNet v21~~ - **STALE, closed 2026-09-02**: the founder ruled 2026-08-29 "v1 ships TrackNet" (pm agent memory `founder-rulings-2026-08-29.md`) - BallNet v21 stays the **desktop** pipeline's default (a separate product), mobile correctly bundles the model v1 actually ships. Not a defect. And `modules.md` overclaimed (corrected) | [evidence/mobile-viability-audit.md](evidence/mobile-viability-audit.md) |
| **P0-3 crop-around-contact - contact sheets REVIEWED 2026-08-29, result stands** | Rebuilt with a homography-free population and a person-specific test. **Pre-registered strict test (box grown 25% must contain the ball anchor): full frame @1280 = 0/25, crop192@640 (yolo11x) = 2/25.** The often-quoted **15/25 is POST-HOC**, from the relaxed criterion (a far-sized non-near person found anywhere in the crop) - never quote it unlabelled. **Human review of `p0_3_context_yt_match40.png` (25 full-frame tiles): both strict passes are real far-end figures, distinct from the yellow near-player box; every rejection sampled is a real far player thrown out for anchor distance, not a bad detection** - so the crop finds the far player and the strict test measures anchor precision. Two contacts (#12, #38) are near-end contacts misclassified far - the population's own 23/25 not-on-near-player figure. The variable is the **upscale factor** (peak ~100-140 px of player in the tensor), not the crop size. A ball-centred 192px crop holds the far player only barely - **median 26.3 px from the crop edge**. `am_hard_utr` (n=12) is UNDERPOWERED and its population is contaminated | [evidence/p0-3-crop-around-contact.md](evidence/p0-3-crop-around-contact.md) |
| **`data/yt_match40_pts.json` is MISCALIBRATED** - found 2026-08-28 | A human to re-click the corners. RECORDED, NOT FIXED (rule 9). All four clicked corners sit on run-off asphalt, hedge and fence; the audit stamps it PASS at 0.9 px because a residual only asks whether four points form a plausible court. Every `yt_match40` far-player number and its whole shot list inherit it | [evidence/yt-match40-calibration-is-wrong.md](evidence/yt-match40-calibration-is-wrong.md) |
| **Phone app shell** | App development, not ML. No phone fps has ever been measured, so do not quote one | [evidence/mobile-viability-audit.md](evidence/mobile-viability-audit.md) |

---

| ~~**`mobile/live_calls.js` doubles-alley bug**~~ - found 2026-09-02, FIXED + EXERCISED 2026-09-02 | **Nothing. Fixed.** `_detectBounce` called `isInSingles` unconditionally regardless of `this.singles` - in doubles mode an alley ball (inside doubles, outside singles) was called **OUT while the displayed margin read positive/inside**, and it is legally IN in doubles. `live.py` was already correct (its ternary branches on `self.singles`); only the port had drifted, and JS had no `isInDoubles` at all to branch to. Fixed: added `isInDoubles`, `_detectBounce` now branches like the Python. **Exercised, not just fixed** - the existing parity check is singles-only and produced zero OUT calls, so it could not see this bug. New `mobile/doubles_alley_parity_cases.json` (21 synthetic court positions, hand-computed expected, independent of both implementations) + `backend/live_doubles_alley_probe.py` + `mobile/verify_live_doubles.js` drive the full bounce state machine (identity homography, not `isInSingles`/`isInDoubles` in isolation - the bug was in the wiring) in both modes: **42/42 match** the hand-computed expectation on both sides, including all 7 alley-asymmetric cases. **Python checked independently, not assumed correct**: 42/42 also match on the Python side alone - no findings against the reference. Confirmed non-vacuous: reverting the fix and re-running fails exactly the 7 doubles-mode alley cases. | [evidence/doubles-alley-live-call-bug-fixed-and-exercised.md](evidence/doubles-alley-live-call-bug-fixed-and-exercised.md) |
| ~~**`mobile/ball_detector.js` decode disagreed with `ball.py` on multi-blob frames**~~ - found + FIXED 2026-09-02 | **Nothing. Fixed.** Input-side channel packing (3-frame stack, BGR order, buffering) was already bit-exact - `_buildInput()` vs Python's tensor: 0.00 max abs diff. The DECODE algorithm was not: `ball.py`'s `_postprocess` does full-image 8-connected-component analysis scored by area x peak; `ball_detector.js`'s old `_decode` instead took the global-argmax pixel and a fixed +-3px window - a different algorithm despite the file's own "mirrors ball.py" docstring claim. Measured on 178 real contiguous frames from `data/incoming/Hardcourt/am_hard_utr.mp4` (61 real TrackNet detections, both PyTorch and the real bundled fp32 ONNX graph): 100% null-agreement but only **57/61 (93.4%) position agreement at a pre-registered <=5px bar**, 4 gross mismatches up to **238px** - every one a frame with two separate heatmap blobs, JS locking the wrong one. Passed the pre-registered bar but is a confident-wrong-lock failure mode with no refusal signal, so fixed rather than left: ported the same connected-component + area x peak algorithm into `_decode`. Re-verified on the same real frames, same real ONNX graph: **61/61 exact, 0.00px**. Confirmed non-vacuous (`git stash` the fix, re-run, reproduces the original 4 mismatches exactly; restored). Separately, the 2026-08-27 "mobile bundles the wrong detector" defect note (row above) is now closed as STALE - the founder ruled 2026-08-29 v1 ships TrackNet. | [evidence/ball-detector-parity-tracknet.md](evidence/ball-detector-parity-tracknet.md) |
| ~~**`mobile/models/tracknet_ball.int8.onnx` (the quantised graph) - PARTIALLY measured**~~ - 2026-09-02 | **SUPERSEDED 2026-09-03 by the six-clip run** (Open row above). The partial verdict was 51 of 178 clip-start frames on ONE clip and passed all three bars; the full run on that same clip failed, and 2 more clips have since failed. Its own hedge - *not yet a fully powered verdict, needs the full 178* - was correct, and is the reason nothing shipped on it | [evidence/ball-detector-parity-tracknet.md](evidence/ball-detector-parity-tracknet.md) |

## Recently closed

Answered, and kept only as a pointer so the answer is not re-derived.
Full text in [archive/resolved/](archive/resolved/).

| Item | Answer | Evidence |
|---|---|---|
| **Court auto-detection** | CLOSED AGAIN 2026-08-25 - five branches measured, none survives. The detector FINDS the court's lines but cannot assemble them. The correct next build is joint line-to-model correspondence, and it was NOT tested | [archive/resolved/court-auto-detection.md](archive/resolved/court-auto-detection.md) |
| **Court auto-detection as a MASK problem** | Superseded by the row above | [archive/resolved/court-auto-detection-2.md](archive/resolved/court-auto-detection-2.md) |
| **Whether the +5.6 pt recall gain reaches the product** | ANSWERED 2026-08-13: it does not. Solid ghosts 9 -> 13 at flat chain recall | [archive/resolved/whether-the-5-6-pt-recall-gain.md](archive/resolved/whether-the-5-6-pt-recall-gain.md) |
| **Speed coverage as detector work** | Superseded - it is CHAIN work; see the Open row | [archive/resolved/speed-coverage.md](archive/resolved/speed-coverage.md) |
| **Rally segmentation / score** | **SUPERSEDED - this row was STALE.** It was ruled out 2026-08-20, then ruled BACK IN 2026-08-27 (CLAUDE.md rule 12). It is a work item again, and its blocker is not scope but GROUND TRUTH: rule 11 bars the scoreboard/HUD, so a compliant source (human-clicked point boundaries, or boundaries from bounces and physics) is a prerequisite for any number here. `stats.score_validation_note` stays | [archive/resolved/rally-segmentation-score.md](archive/resolved/rally-segmentation-score.md) |
| **Processing 60 fps clips at full rate** | DECIDED 2026-08-15: shipped opt-in as `--full-rate` | [archive/resolved/processing-60-fps-clips-at-full-rate.md](archive/resolved/processing-60-fps-clips-at-full-rate.md) |
