# Generated by CI (`xcodegen generate` + `xcodebuild`), never committed.
# The whole point of using XcodeGen is that the .xcodeproj is reproducible from
# project.yml — committing the generated project would let the two drift apart.
LatencyHarness.xcodeproj/
build/
Payload/
*.ipa
DerivedData/
